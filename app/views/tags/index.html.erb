<h1>All Tags</h1>

<ul id="tags">
    <% @tag.each do |tag|%>
        <li>
            <%= tag.name %>
            <%= link_to "delete", tag_path(tag.id), method: :delete, data: {confirm: "Really delete the tag?"} %>
        </li>
        
    <%end%>
    
</ul>